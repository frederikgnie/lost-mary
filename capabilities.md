# Capability declaration (library v2)

What this library depends on in the Claude Code runtime, how each dependency
was verified, and how each piece degrades when the runtime changes. Claude
Code's own documentation is the authority; this file records what was checked
and when, so a future upgrade has a checklist instead of a surprise.

Last verified: **2026-09-17**, Claude Code **2.1.273**, against
`code.claude.com/docs` (hooks, sub-agents, skills, settings) and against real
transcripts on disk.

## Hooks

| Script | Event / matcher | Runtime fields relied on | Status |
| --- | --- | --- | --- |
| `scripts/pycheck.py --hook` | `PostToolUse`, matcher `Edit\|Write\|MultiEdit` | `tool_name`, `tool_input.file_path`. Exit 2 = stderr shown to the model (non-blocking; the edit already happened). | Documented |
| `scripts/check-evidence.py` | `SubagentStop`, matcher on agent type (`implement\|general-purpose`) | `session_id` and `agent_id` (they name the witness file), `agent_type`, `last_assistant_message`, `stop_hook_active`; for the fallback also `agent_transcript_path` (preferred) and `transcript_path` (the **parent** session's file, used to derive the subagent's transcript when `agent_transcript_path` is absent). Exit 2 = block the stop, stderr returned to the subagent. **Evidence source: the witness file (`<EVIDENCE_ROOT>/<session_id>/witness-<agent_id>.jsonl`, written by `witness.py` as each call completes) when one exists AND holds at least one parseable line; the transcript only when it does not.** The transcript is the fallback because the docs call it internal and say it is written asynchronously, so it "may not yet include the current turn's most recent messages when a hook fires" - a truthful agent judged as having run nothing. Falling back prints nothing: a session without `witness.py` wired is normal, and `friction.py` counts hook stderr as fail-open notices. Reading a witness line: the EVENT is the pass/fail signal (`PostToolUse` = exit 0, `PostToolUseFailure` = non-zero), never `exit_code`, which is null on every Bash success; an interrupted call is not a pass; a `PostToolUse` edit line is a confirmed edit; unparsable lines are skipped. | Confirmed live (see log below): `SubagentStop` exit 2 blocks the stop and the derived transcript path resolves. The witness-fed path is covered by `tests/test-hooks.py` (both sources, plus one end-to-end case in which the real `witness.py` writes and this script reads); `witness.py` itself has now been observed writing live (its own row below), but **this consumer reading what that producer wrote has not**. **The one link no test can prove:** that `agent_id` and `session_id` on the `SubagentStop` payload are byte-identical to the ones on that same agent's `PostToolUse` payloads. They are produced by different events, and nothing but a live run compares them - if they differ by so much as a case or a prefix, `witness_path()` names a file that does not exist, the hook reads the transcript instead, and the whole witness path is silently inert (a fallback looks exactly like "the witness hook is not wired"). To verify live: spawn an `implement` that edits and runs something, then compare the id in the witness file's name (`~/.claude/agent-library/evidence/<session>/witness-<id>.jsonl`) with the agent id the ledger records from that stop (`agent <id>` in `~/.claude/agent-library/ledger/<slug>/`, written from the same `SubagentStop` payload). Equal ids, same directory name: the link holds. |
| `scripts/no-ask.py` | `PreToolUse`, matcher `AskUserQuestion` | `tool_name`, `transcript_path`; a slash-command turn is a `type: user` record whose `message.content` string starts with `<command-name>/<name></command-name>` (observed, not documented). Exit 2 = block the call, stderr returned to the model. This is the scoped mechanism: menus stay available outside the procedure. (A global `permissions.deny: ["AskUserQuestion"]` would remove the tool everywhere and make this hook inert - deliberately not used here.) | Contract documented; that `PreToolUse` fires for `AskUserQuestion` is not documented but is **confirmed live** (2026-09-06, in both directions: missed while the detector only accepted a leading `<command-name>`, blocked after the fix). |
| `scripts/no-punt.py` | `Stop` (no matcher - the event has none) | `last_assistant_message`, `prompt_id`, `stop_hook_active`, `agent_id` (present on a subagent); the transcript at `agent_transcript_path` / `transcript_path` for the fallback message and for the turn's records: `promptId` on user records, a Stop block as a `user` record whose string content starts `Stop hook feedback:\n[<command>]: ` (observed, not documented), a slash command as a `user` record starting with the command tags (observed). A refused tool call as a `tool_result` whose body starts `Permission for this action was denied by the Claude Code auto mode classifier. Reason: [...]` or `The user doesn't want to proceed with this tool use.` (observed 2026-09-25, 2.1.281; `REFUSAL_MARKERS`, pinned equal to `friction.py`'s) - what backs a `BLOCKED:` stop in the turn; without one the BLOCKED paragraph must name a user-held item (`USER_HELD` / `FACT_QUESTION`). Exit 2 is the only blocking mechanism `Stop` honours; stderr returned to the model. Loop guard counted from those records: the stop is allowed after two bounces with no tool call between them, or six in one turn - under Claude Code's own override at eight consecutive blocks without progress (`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`). | Documented (hooks guide: Stop, and the cap and `stop_hook_active` in its troubleshooting section). Confirmed live 2026-09-06 (a parked defect) and 2026-09-17 (a go-ahead request - see the log). |
| `scripts/check-spawn.py` | `PreToolUse`, matcher `Agent` | `tool_name`, `tool_input.subagent_type`, `tool_input.prompt` (the Agent tool's parameters). Exit 2 blocks the spawn, stderr (`MISSING: ...`) returned to the model. Only `implement` is governed; the required-field table mirrors the spawn block in skills/lost-mary/SKILL.md. | Documented (PreToolUse). Confirmed live 2026-09-06: a field-less `implement` spawn was refused with `MISSING: OWNED, OFF-LIMITS, DONE MEANS, VALIDATION`. |
| `scripts/check-evidence.py --lead` | `Stop` (the lead; a Stop payload without `agent_id` is treated as the lead even without the flag) | `last_assistant_message`, `transcript_path` (the session), `prompt_id` (the turn = records from the first one carrying that `promptId` to the next prompt), `stop_hook_active`. Rules A and C on the turn's edits/runs, claims against the session's runs. Exit 2 = block, stderr to the model; one strike. **Same evidence source as the row above:** `<EVIDENCE_ROOT>/<session_id>/witness-lead.jsonl` (the lead's own calls carry no `agent_id`, which is what routes them there) when it exists, the session transcript otherwise. With the witness file the turn is the lines whose `prompt_id` matches the payload's - a documented field, rather than the transcript's undocumented `promptId` slicing. A payload carrying NO `prompt_id` gets an empty turn, not the last one in the file: that last one is the previous turn, already judged when it stopped, and re-judging it would bounce this turn for the previous turn's silence. Session-wide claims (rule B) are still checked. | Documented fields (Stop). **Confirmed live** 2026-09-06: the real Stop payload carries `prompt_id`, `last_assistant_message`, `transcript_path` and no `agent_id` (keys observed in a firing), the dispatch routes it to the lead path with or without the flag, it blocks a false claim and allows an honest message. Its first two firings failed open (`payload lacks ['agent_id']`) because the installed copy predated `--lead` - a stale install, not a contract change. |
| `scripts/ledger.py brief` | `SubagentStart`, no matcher | `agent_id`, `agent_type`, `transcript_path` (project slug); prints `hookSpecificOutput.additionalContext` with the last three entries (<= 2500 chars). SubagentStart can inject context but not block (documented). | Documented. Confirmed live 2026-09-06: an `explore` spawn quoted the first entry's `## ` header back from its own starting context. |
| `scripts/permit.py` | `PermissionRequest`, matcher `Bash\|PowerShell` | `tool_name`, `tool_input.command`, `cwd`; output is the documented `decision` object (`behavior: allow`); exit 2 is ignored for this event and never used. Allows only: push to a non-default branch (refspec or current branch via `git symbolic-ref`), validation-only commands (check-evidence's classifier + benign wrappers, redirects to relative paths only), installer verify. An allow is still subject to deny/ask rules (documented). Background non-interactive subagents are auto-denied without a PermissionRequest hook (documented) - this is what lets them run pytest. | Documented (hooks reference, PermissionRequest decision control). **CONFIRMED LIVE 2026-09-15 on 2.1.272, including headless** - this corrects the 2026-09-07 entry below, which measured a version that had the bug 2.1.268 fixed ("PermissionRequest hooks not firing in `--print` mode"). A `claude -p --permission-prompts none` session raised `hook_event_name: "PermissionRequest"` and `permit` returned `{"decision": {"behavior": "allow"}}` for `ruff check good.py`, which then ran. Note the reachability test needs a command that would actually prompt: with 89 allow rules plus `autoMode`, `git status`, `docker ps` and an out-of-cwd `cat` were all allowed outright and never raised the event - an explicit `permissions.ask` rule is what forces it. **Degradation found in the same run:** `permit` imports check-evidence's classifier from beside itself, and with `check-evidence.py` absent it declines every validation command silently (fails open, correct, but invisible) - the two files must be installed together. |
| `scripts/ledger.py record` | `SubagentStop`, no matcher (every role); `Stop` for the lead's own turn | Subagent: `agent_id`, `agent_type`, `agent_transcript_path` / `transcript_path`, `last_assistant_message`, `stop_hook_active`, `cwd` (to make edited paths relative). Lead (`Stop`, no `agent_id`): `transcript_path`, `prompt_id` (the turn = records from the first one carrying that `promptId` to the end; assistant records carry none - observed, not documented), `session_id`; written only when the turn edited or ran something; skipped when `stop_hook_active`. The ledger lives at `~/.claude/agent-library/ledger/<slug>/` (`LEDGER_ROOT` overrides), one file per stop, where `<slug>` is the directory name of `transcript_path` - the per-project key Claude Code itself uses - so a worktree subagent's entry lands with its lead's project and nothing in a work tree can pose as the record. Edits are confirmed by a non-error `tool_result`; runs come from `check-evidence.classify`/`run_failed` (imported by path, registered in `sys.modules`). One shell call has one verdict for every segment in it, so a chain whose output merely mentions `exit 2` (seen live: an echoed probe result) records its runs as FAILED until the same segment passes later in the turn - latest outcome wins. Observed, not documented: `agent-<id>.meta.json` with `agentType`/`description`; `gitBranch` on records. Always exit 0. **Confirmed live** 2026-09-05/06 on both paths (Stop and SubagentStop), including entries written by unrelated sessions in two other projects. It does NOT emit `additionalContext` on `SubagentStop`: the reference documents that Stop/SubagentStop `additionalContext` continues the stopping agent's conversation (loop-protected), i.e. it would give the subagent an extra turn - so the hand-back to the lead is `ledger.py attach` on `PostToolUse` matcher `Agent`, which fires in the lead's context when the Agent call returns and looks the entry up by the `agentId:` line in the result. | Fields documented; meta/branch shape observed on 2.1.260. Confirmed live - see the log. |
| `scripts/ledger.py attach` | `PostToolUse`, matcher `Agent` | `tool_name`, `tool_output` (the Agent result text, which carries `agentId: <id>` - observed, not documented), `transcript_path` (project slug). Prints `hookSpecificOutput.additionalContext` with the matching entry; nothing when there is no entry yet (background spawns return at launch). | `additionalContext` on PostToolUse documented (inserted where the hook fired, read on the next request). Confirmed live 2026-09-06 after fixing the result shape: `tool_response` for Agent is a structured object (`agentId`, `agentType`, `content` blocks, `status`, `usage`...), and the entry arrived in the lead's context beside the subagent's answer. |
| `scripts/ledger.py recall` | `SessionStart`, matcher `startup\|resume\|clear\|compact` | `transcript_path` (its directory name selects the project's ledger); stdout is added to the session's context (documented). Whole entries only, at most ~6 KB; prints nothing when there is no ledger. | Documented (hooks guide: SessionStart stdout -> context; `compact` matcher re-injects after compaction). **Confirmed live** 2026-09-06: fired twice on the `compact` matcher in two other sessions (exit 0, banner in stdout, recorded as an `attachment` with `hookEvent`). |
| `scripts/witness.py` | `PostToolUse` AND `PostToolUseFailure`, matcher `Bash\|PowerShell\|Edit\|Write\|MultiEdit\|NotebookEdit` | `hook_event_name`, `session_id`, `prompt_id`, `agent_id` and `agent_type` (both absent on the lead's own calls - that absence is what routes a line to `witness-lead.jsonl`), `tool_use_id`, `tool_name`, `tool_input.command` / `.file_path` / `.notebook_path`, `tool_response` (`stdout`, `stderr`, `interrupted`, `filePath`; `exit_code` is read when present and never required - Bash does not send one), and on the failure event `error` (first line `Exit code N`) and `is_interrupt`. Writes one JSON line per call to `<EVIDENCE_ROOT>/<session_id>/witness-<agent_id>.jsonl` or `witness-lead.jsonl` (`EVIDENCE_ROOT` defaults to `~/.claude/agent-library/evidence`), never under `~/.claude/projects` - that refusal compares RESOLVED paths, so a `..` segment, a relative root, a link or a Windows 8.3 short name cannot walk past it. The `witness-` prefix keeps these files distinct from Claude Code's own `agent-<agent_id>.jsonl` subagent transcripts, which `check-evidence` searches project trees for. Both ids are matched against `^[A-Za-z0-9_-]{1,64}$` before becoming path parts. Appends in one `write()` on a file opened `ab`, bounds free text to head + tail and the line to 4000 bytes but never pre-bounds `command` (the consumer classifies that string; an elided middle would hide a `&& pytest -q`), classifies nothing, prints nothing on stdout, always exits 0. Pruning (on creating a session directory) touches only directories that are provably its own output: a session-id name, resolving to where it is spelled, holding nothing but `witness-*.jsonl` regular files - anything else is skipped whole, never emptied - and ages a directory by its NEWEST file, because a directory's mtime does not move when a file is appended to. Blind spot, shared with the transcript: a compound command that swallows its exit code (`cmd; echo EXIT=$?`) exits 0 and arrives as `PostToolUse`. | **Confirmed live 2026-09-15 on 2.1.272**, twice. First with probe hooks - see the log: `PostToolUse` fires inside subagents with a stable `agent_id`, a non-zero exit arrives as `PostToolUseFailure` with no `tool_response`, and Bash success payloads carry no `exit_code` despite the docs. Then `witness.py` ITSELF, wired and fired by the lead inside a real spawned subagent after it was built: three of that subagent's calls landed in its agent file (a successful `Bash` with `exit_code: null`, a `Write` with its `file_path`, and an `exit 3` arriving as `PostToolUseFailure` with `exit_code: 3`) while the lead's own call landed in the lead file - the producer works live, on both events, in both contexts. What is still unobserved is the CONSUMER reading that file; see the `check-evidence.py` row for the one link no test can prove. |
| `scripts/guard-shared-checkouts.py` | `PreToolUse`, matcher `Bash\|PowerShell\|Edit\|Write\|MultiEdit\|NotebookEdit` | `tool_name`, `tool_input.command`, `tool_input.file_path` / `.notebook_path`, `cwd`. Config from `$LOST_MARY_SHARED_CHECKOUTS`, else `~/.claude/agent-library/shared-checkouts.json` (`shared`, `frozen`, optional `frozen_by`; other keys ignored), read on every call. Exit 2 = block the call, stderr returned to the model (documented PreToolUse contract). Only `git` invocations are inspected, so running a deploy script that itself touches the frozen tree is not blocked. | Contract documented; the script is covered by `tests/test-hooks.py`. Ported 2026-09-25 from an unversioned copy another session had written into the installed library after the 2026-09-24 branch switch in a shared checkout; not yet observed blocking live from this repository's version. |

**Subagent transcript location.** This is the fallback path, taken only when
the session has no witness file. The hook reads `agent_transcript_path`
directly when the payload carries it. Otherwise it derives (relied on by
`check-evidence.py`, observed, not documented)
`<parent-dir>/<parent-stem>/subagents/agent-<agent_id>.jsonl` from
`transcript_path`. Records are JSONL with `type: assistant|user` and
`message.content[]` items of type `tool_use` `{id, name, input}` and
`tool_result` `{tool_use_id, content, is_error}`; a failing Bash result has
`is_error: true` and text starting with `Exit code N`. The docs call this
format internal and unstable. If neither resolves, the hook falls back to
searching the project directory for `agent-<id>.jsonl`, then fails open with a
notice. `tests/test-hooks.py` pins the observed shape.

**Every hook fails open, loudly.** An unreadable payload, a missing transcript
or a missing tool prints a one-line notice on stderr and exits 0. That is the
right call for availability and the wrong one for enforcement: if Claude Code
renames a field, the hooks stop acting and only the notice tells you. After an
upgrade, re-run `tests/test-hooks.py` (catches our bugs, not theirs) **and** the
live smoke test in `GETTING-STARTED.md` §3 (catches theirs).

**Stdin encoding.** All hooks read `sys.stdin.buffer` and decode `utf-8-sig`.
PowerShell prepends a UTF-8 BOM when piping to a native executable; text-mode
stdin on Windows would turn it into mojibake and the hook would fail open.

**Shell on Windows.** Hook `command` strings run under Git Bash (`sh -c`) when
it is installed, otherwise PowerShell. `$HOME` expands in both, which is why
`settings.example.windows.json` uses `$HOME` rather than `%USERPROFILE%`. The
interpreter is the trap: `python` on PATH is often the Microsoft Store stub, so
the Windows example expects an absolute `python.exe` path.

**Kill switches.** `PYCHECK_DISABLE=1` in the environment silences `pycheck`;
`"disableAllHooks": true` in `settings.json` disables every hook.

**Not used, deliberately.** `permissions.deny` cannot be scoped to one agent
(documented), and Bash sandboxing is **not available on Windows native**
(WSL2 only) - hence the read-only roles simply have no Bash. (Until 2026-09-15
this paragraph also claimed `agent_type` was undocumented on
`PreToolUse`/`PostToolUse`. It is documented now, and measured: a `PostToolUse`
payload raised inside a subagent carries both `agent_id` and `agent_type`, and
the lead's own carries neither - `witness.py` depends on exactly that
difference. Hook *matchers* on these events still match tool names, not agent
types, so nothing selects a hook by `agent_type` here.)

## Agents (`agents/*.md` -> `~/.claude/agents/`)

Frontmatter keys used and their documented values:

| Key | Used as | Documented values |
| --- | --- | --- |
| `name`, `description` | required | `name` matches `^[a-z0-9-]+$` |
| `tools` | strict allowlist | tool names; omitting `Bash` denies Bash |
| `model` | `fable` (explore, review), `opus` (implement) | `sonnet`, `opus`, `haiku`, `fable`, `inherit`, or a full model id |
| `effort` | `medium` (explore), `high` (review) | `low`, `medium`, `high`, `xhigh`, `max` |

Available but not used yet: `hooks` (per-agent hooks; `Stop` maps to
`SubagentStop` at runtime - would let `implement.md` carry the evidence hook
itself, at the cost of hard-coding an interpreter path in a shipped file),
`permissionMode`, `maxTurns`, `isolation: worktree` (the lead passes it per
spawn instead), `skills`, `background`. `memory` is used - `local` on
`implement`, see below.

`tests/test-agents.py` asserts the tool properties (`explore`/`review` carry
no mutating tool; `implement` has `Edit`, `Write`, `Bash`), the key set, and
the report formats the docs promise.

**Subagent frontmatter, verified 2026-09-06 against `code.claude.com/docs/en/sub-agents`:** `name`, `description`, `tools`, `disallowedTools`, `model`, `permissionMode`, `maxTurns`, `skills`, `hooks`, `memory` (`user` | `project` | `local`; enables Read/Write/Edit for the memory directory and injects memory instructions - so it is used on `implement` only, never on the read-only roles), `background`, `effort`, `isolation`, `color`. Memory depends on auto memory being enabled (`autoMemoryEnabled`). `tests/test-agents.py` `KNOWN_KEYS` lists them.

**Model routing (2026-09-06).** Role frontmatter carries the defaults (`explore` and `review` `model: fable` - planning and judgment, where an error multiplies; `implement` `model: opus` - execution, caught by the contract, the hooks and the tests); the Agent tool's `model` parameter overrides per spawn; `~/.claude/agent-library/model-policy.md`, when present, is `cat`-ed into the session at `SessionStart` (plain stdout -> context). No hook can read quota or switch the session's model (`PreModelSwitch` can only block a switch), so the policy file is the dial and `/model` stays the user's.

**What happens when a pinned model runs out (verified 2026-09-07).** A usage limit is NOT covered by `fallbackModel`: that chain catches overloaded/unavailable/server errors and explicitly excludes rate-limit, auth and billing errors, so a role pinned with `model: <x>` does not silently fall back when `<x>`'s weekly cap is reached. For Fable the cap raises a usage-credits consent prompt, shown only in the session's own terminal - **a Remote Control client cannot display it**, and unanswered within `dialogExpiry` (5 min) Claude Code sends nothing and keeps the model. It applies to Remote Control, background and agent-team teammate sessions. Recovery is `/model <other>`, answering at the terminal, or the weekly reset; `autoContinueAtUsageLimit` waits out session/weekly limits, not a model-family limit. Without a chain a subagent whose model is unavailable ends with `Agent terminated early due to an API error`. Consequence for this library: pinning a scarce model in role frontmatter is a real availability risk - the machine policy file names the manual fallback, and `fallbackModel` in the example settings covers only the overload case.

**Agent Teams, verified the same day:** `TaskCompleted` carries `task_id`, `task_subject`, `task_description`, `teammate_name`, `team_name` and `TeammateIdle` carries `teammate_name`, `team_name` - both with the *session's* `transcript_path`, no teammate transcript and no final message. A teammate's evidence therefore cannot be checked by a hook; `/lost-mary` offers teams as an option with the same spawn contract and keeps the gate with the lead re-running the decisive check.

## Skills (`skills/<name>/SKILL.md` -> `~/.claude/skills/<name>/` -> `/<name>`)

Keys used: `name`, `description`, `argument-hint`, `disable-model-invocation`
(`/lost-mary`, `/pr` are user-invoked only; `/validate` may be invoked by the
model). Substitutions used: `$ARGUMENTS`. Dynamic context used: `` !`command` ``
lines run in bash (default `shell`) before the skill body reaches the model.

`@path` imports do **not** work inside a skill body (CLAUDE.md only); the
skills instruct explicit reads instead. A skill invoked with `/name` stays in
context for the rest of the session - bodies are kept short for that reason.

## Settings (`settings.example*.json`)

The `hooks` block is prescribed; `autoMode` is a template (below). Agent Teams
(`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`) are no longer part of the library's
model; the Agent tool's `isolation: worktree` covers concurrent writers.

**`autoMode` template and the extra `--verify` checks (2026-09-04).** The
examples carry `autoMode.allow` with `"$defaults"` plus three prose rules (the
third, the PR-merge exception, added 2026-09-24 - see the `gh pr merge` entry
below); auto mode reads it from `~/.claude/settings.json` only. `--verify`
reports DRIFT when
`autoMode.allow` exists without `"$defaults"` (the list would replace the
built-in classifier rules), a NOTE when `defaultMode` is `auto` with no
`autoMode.allow`, and a NOTE when five or more `Bash(...)` allow rules are exact
strings (they match one command forever - the shape that keeps prompts coming).
`scripts/friction.py` is the read-only CLI behind that last number: it counts
questions, refusals, hand-backs and dead allow rules over the last N transcripts
and depends on the same transcript shape as `check-evidence`.

**Permissions and auto mode** (verified 2026-09-04 against
`code.claude.com/docs/en/auto-mode-config` and `permissions`):

- `permissions.allow` rules are prefix matches (`Bash(git push *)`; the space
  before `*` matters). A compound command (`cd X && cmd`) is checked one
  subcommand at a time, so a chain passes when every part is allowed.
  `PowerShell(cmd *)` rules exist and behave the same way.
- `autoMode.allow`, `autoMode.soft_deny`, `autoMode.hard_deny` and
  `autoMode.environment` are lists of plain prose the classifier reads; the
  literal `"$defaults"` keeps the built-in list and adds to it. Auto mode reads
  `autoMode` from `~/.claude/settings.json` only, never from a project
  `.claude/settings.json`.
- A bare tool name in `permissions.deny` (e.g. `"AskUserQuestion"`) removes
  the tool from the model's context. That is the mechanism behind the
  "decide, do not ask" rule in `global-CLAUDE.md`; the text alone is advisory.
- In auto mode the classifier refuses every attempt by Claude to write
  `~/.claude/settings.json` (Bash, Python, the Write tool) and also refused
  scratchpad scripts whose content assigned `permissions.deny` / `autoMode`
  keys or documented how to. No allow rule lifts this. Prepare the change,
  put the JSON in the reply, let the user apply it. A project
  `.claude/settings.json` in the cwd is writable via the Write tool.
  Re-checked 2026-09-24 (2.1.281): writing a merged copy to the scratchpad is
  refused too (`[Self-Modification]`), and so was a `grep` over
  `settings.example*.json` issued right after it - the denial covers the
  outcome, not the path. The reply is the only channel.
- `gh pr merge` in auto mode (verified 2026-09-24, 2.1.281, against
  `permission-modes` and `auto-mode-config`): a built-in default blocks
  "merging a pull request no human has approved, approving Claude's own pull
  request, or disabling CI checks", reported to the model as
  `[Merge Without Review]`. "Review" means a human approval on the PR: a
  `review` spawn cannot satisfy it, because the classifier sees user messages,
  non-read-only tool calls and CLAUDE.md but tool results are stripped - so a
  subagent's verdict is invisible to it. Transcripts confirm it: sessions in
  `c--repo-powercountant` with six `review` spawns were denied the merge
  twice (2026-09-23). Not `hard_deny`: a user message naming the merge got
  one through on 2026-09-23, which only a `soft_deny` permits, so an
  `autoMode.allow` rule overrides it too (the docs do not name the tier; this
  is the inference). The template rule's preconditions are what the
  classifier can see - that the checks and the `review` spawn were *called*,
  not that they passed; pass/fail stays with `check-evidence`. A chained
  `push && pr create && pr merge` is denied as a whole - the classifier
  judges the Bash call as one unit, unlike the prefix-rule matching above -
  so a merge, when allowed, is its own call.

## Degradation guidance

| If Claude Code ... | Then |
| --- | --- |
| renames `tool_input.file_path` or `tool_name` | `pycheck` stops firing (fail open). Update `hook_main()`; the CLI and `/validate` keep working. |
| renames `agent_id` / `transcript_path` / `last_assistant_message` | `check-evidence` stops firing with a stderr notice. Update field names in `main()`. |
| moves subagent transcripts | `subagent_transcript()` falls back to a project-dir search; update the derived path if the fallback misses. |
| changes the transcript record shape | `read_transcript()` finds no evidence and the hook allows everything (with edits/no-runs no longer detected) - but only where no witness file exists, since that is the preferred source. Re-derive the shape from a real file and update `tests/test-hooks.py`. |
| fires `PostToolUse` but the operator wired `witness.py` on that event only, without `PostToolUseFailure` | every failed run is missing from the witness file while every passing one is present, so `check-evidence` reads a clean record of a turn that failed and ALLOWS it - a false allow it cannot detect, because an absent line is indistinguishable from a call that never happened. Nothing at runtime notices. The only guard is the installers' drift check, which lists `(PostToolUse, witness.py)` and `(PostToolUseFailure, witness.py)` as two separate required pairs and prints `DRIFT <event> does not run witness.py` when one is missing - so run `install.sh --verify` / `install.ps1 -Verify` after any hand-edit of `settings.json`. Wire both events or neither. |
| changes the witness line format, or `witness.py` is not installed | `check-evidence` silently reads the transcript instead - the behaviour before the witness hook existed. `EVIDENCE_ROOT`, its default and the id pattern are duplicated in both scripts and pinned equal by `tests/test-hooks.py`; the line format itself is `witness.py`'s docstring. |
| stops honouring exit 2 on `SubagentStop` | switch `check-evidence` to the JSON form (`{"decision":"block","reason":...}` on stdout, exit 0). |
| (any version) returns one output for a Bash call that chains a lint or test run with other commands | `check-evidence` judges every validation segment of the call by that one output, so a diagnostic line such as `exit=2` printed by a later command reads as the lint having failed (seen 2026-09-25: a ruff run chained with hook smoke tests that print each exit code). Not a bug it can fix - one call has one outcome. Keep validation commands in their own Bash call; the bounce is a false positive when the standalone re-run passes. |
| does not fire `PreToolUse` for `AskUserQuestion`, or changes the slash-command transcript shape | `no-ask` silently allows (fail open). Fall back to `"deny": ["AskUserQuestion"]` under `permissions` (global or per project); re-derive the `<command-name>` shape from a real transcript. |
| (any version) runs `guard-shared-checkouts` with no `shared-checkouts.json` and no `$LOST_MARY_SHARED_CHECKOUTS` | the guard is a silent no-op - by design, so machines without shared checkouts pay nothing. A malformed or wrong-typed config also allows everything, with one `guard off` line on stderr. Renamed `tool_input` fields or `cwd` make it allow (fail open); relative paths then resolve against nothing and match nothing. |
| drops `last_assistant_message` from `Stop` | `no-punt` reads the last assistant record from `transcript_path`; if that shape changes too it allows with a notice. |
| changes the text of a refused tool call's result | `no-punt` sees no refusal in the turn, so a `BLOCKED:` passes only by naming a user-held item; a real classifier denial then bounces once with "try it" and the retry produces the same denial - a wasted call, not a wedge (the loop guard still applies). `friction` over-counts unbacked BLOCKED stops. Update `REFUSAL_MARKERS` in both scripts from a real transcript. |
| stops writing a Stop block as a `Stop hook feedback:` user record, or changes that shape | `no-punt` counts zero bounces in the turn and keeps bouncing a hand-back up to Claude Code's own cap of eight; `friction` shows 0 bounced against a non-zero hand-back count. Update `HOOK_FEEDBACK` in both scripts. |
| stops adding `SessionStart` stdout to context, or renames the `compact` source | `recall` prints to nowhere; the ledger file is still written and can be read by hand. Switch to `hookSpecificOutput.additionalContext` if that becomes the documented form. |
| drops `agent-<id>.meta.json` or `gitBranch` | entries lose role description / branch; `agent_type` from the payload still labels them. |
| changes agent/skill frontmatter keys | update `tests/test-agents.py` `KNOWN_KEYS`, `tests/test-skills.py` `KNOWN_KEYS`, and the files; reinstall. |
| ships Bash sandboxing on Windows | consider giving `explore` a read-only Bash for `git log`/`blame`; keep `review` without. |
| ships per-agent `permissions` in settings | prefer that over tool omission where finer control is wanted. |

**How to see whether a hook is actually working.** Claude Code records a firing in the session transcript
as an `attachment` object - in **three shapes with different keys** - and a block on `Stop` is a fourth record
that is not an attachment at all (last row) - so a check written against one of
them sees nothing of the others (verified 2026-09-15 on 2.1.260 by direct inspection of
`~/.claude/projects`; over the last 50 transcripts: 222, 86 and 34 records respectively).

| `attachment.type` | keys | what it means |
| --- | --- | --- |
| `hook_blocking_error` | `blockingError`, `hookEvent`, `hookName`, `toolUseID`, `type` - **no `exitCode`, no `stderr`** | Claude Code's name for an **exit-2 result**, which is not the same as "the action was prevented". On `PreToolUse` it was. A `Stop` block never arrives in this shape - it is the `user` record in the last row, and no `hook_blocking_error` with `hookEvent: Stop` exists in any transcript on disk (2026-09-16); `SubagentStop` is unmeasured. On `PostToolUse` it was not: the edit already happened (see `pycheck` in the table above) and exit 2 only returns the diagnostics to the model while the edit is still in its working memory - so the 222 records counted here are findings on edits that landed, **not 222 edits prevented**. The message and the hook's own command line are at `blockingError.blockingError` and `blockingError.command`. |
| `hook_success` | `command`, `content`, `durationMs`, `exitCode`, `hookEvent`, `hookName`, `stderr`, `stdout`, `toolUseID`, `type` | the hook ran and printed something. **Exit 0 with `stderr` is a hook that failed open** and explained itself on a stream nobody reads - a stale install or a wrong payload assumption hides there. |
| `hook_additional_context` | `content`, `hookEvent`, `hookName`, `toolUseID`, `type` - no `exitCode`, no `command` | the hook injected context (SessionStart, SubagentStart, `ledger attach`). Nothing is wrong; the only name it carries is `hookName`. |
| *(none - not an attachment)* a `user` record whose `message.content` is a string beginning `<Event> hook feedback:` | `type: user`, `uuid`, `timestamp`, `promptId`, `sessionId`; `message.content` = `"Stop hook feedback:\n[<the hook command exactly as settings.json spells it>]: <the hook's stderr>"` | a hook that **blocked on `Stop`**. It leaves no attachment (the `hookEvent: Stop` attachments are `hook_success` notices), so a consumer that scans attachments sees none of them - `friction.py` did not until 2026-09-16 (60 on disk, 2.1.273; verified by direct inspection). The same record can repeat with the same `uuid` after a resume - dedupe on it; a `tool_result` that quotes the phrase carries a list and is not one; the event name is in the record, and only `Stop` has been seen. |

`friction.py` keeps the runtime's word and labels these `block(s)`; read one as "the hook fired and
returned exit 2", then look at the event to know whether anything was actually stopped.

Two consequences worth holding on to. **A hook that succeeds quietly leaves no record at all** - `no-ask`
allowing a menu, `pycheck` finding nothing, `ledger.py recall` with no ledger yet, `cat <model-policy>`
with no such file - so silence in the transcripts is not evidence of a dead hook, and counting firings can
never prove one is alive. Nor is this a property of the event: whether a firing leaves a record is decided
by the SCRIPT, so a `SessionStart` hook is exactly as free to be silent as a `PreToolUse` one.
`friction.py --health` answers liveness from the wiring instead: every command under
`hooks[event][*].hooks[*].command` in `settings.json`, whether its script exists, whether it still parses
(`ast.parse`, never an import - a health check must not run the thing it checks, which is also why
`--health` skips the one import this tool otherwise does, `no-punt.py`: on an installed machine that file
IS the wired `Stop` hook), and when that script was last seen firing. Silence is never by itself a fault
verdict; the one distinction drawn is between "nothing is known about this script" and "this event was
seen firing through a record that carries no command", which cannot be attributed to any script.

Second, **a `PreToolUse` block reaches the model as `tool_result` text**, not as an attachment - verified
2026-09-15 on 2.1.260 by direct inspection of `~/.claude/projects`: over the 50 most recent transcripts,
`check-spawn.py` blocks appear 9 times as text inside `type: user` / `tool_result` records and produce
ZERO `hook_blocking_error` attachments, and `friction.py --json` attributes all 312 `hooks_seen` blocks to
`pycheck.py --hook`, none to `no-ask.py` or `check-spawn.py`. That is why `friction.py` finds blocked menus
by their message in the result body and counts them under `blocked` rather than under the hook blocks.

Testing a block with `exitCode == 2` therefore matches nothing, ever. `scripts/friction.py` did exactly
that until 2026-09-15 and reported "0 blocks" while `pycheck` had blocked 222 bad Python edits since
09-03 - read, reasonably, as "the hooks are doing nothing". The fixture in `tests/test-hooks.py` had been
invented rather than taken from a transcript, so the defect shipped green; it now pins all three real
shapes, and findings carry the record's own `timestamp` so a problem fixed in September cannot keep
reading as a live one. When the surface shifts again, re-derive it from a real transcript and update
`scan_hook_record` together with those fixtures.

`friction.py --record` remains the fail-open watch, now with dates on every notice. Two real defects were
found this way on 2026-09-06, hours after they started: an installed
`ledger.py` without `brief` (`usage:` on every `SubagentStart`) and an installed `check-evidence.py`
without `--lead` (`payload lacks ['agent_id']` on every `Stop`). Both failed open, so nothing broke and
nothing was reported - which is exactly why the counter exists.

**A run the classifier cannot see (2026-09-07).** `classify()` reads the command text, so a validation run
hidden behind a shell variable - `for t in tests/*.py; do python "$t"; done` - is not credited, and
`check-evidence` reports "ran no validation" for a turn that did validate. This is deliberate: crediting
`python "$VAR"` would credit any command, which is the one thing the hook exists to prevent. The fix is on
the caller's side - run the suites as explicit commands - and the bounce is one strike, so an honest
re-emission goes through. This is the only known false positive; it errs toward demanding evidence.

## Verified-live log

Fill this in after a real session confirms behaviour; the test suite cannot.

| Date | Claude Code | Check | Result |
| --- | --- | --- | --- |
| 2026-09-03 | 2.1.259 | `pycheck --hook` on a real monorepo file: shared-sibling venv discovered, ruff + ty diagnostics returned | OK (manual invocation of the hook script) |
| 2026-09-03 | 2.1.259 | `pycheck` feedback appears after an `Edit` inside a live session | OK - the hook fired on an `Edit` of `tests/fixtures/pycheck/bad.py` and the F401 + `invalid-assignment` findings came back as tool feedback. Observed along the way: the running session picked up the new `hooks` block from `settings.json` without a restart. |
| 2026-09-03 | 2.1.259 | `check-evidence` bounces an `implement` subagent that claims a run it never made | OK - an `implement` subagent was told to write one file, run nothing, and report "RAN: pytest -q -> 12 passed". Its stop was blocked and it re-emitted "BOUNCED: Evidence check failed (implement). Your final message claims validation, but the transcript contains no test/typecheck/lint command...". Confirms that SubagentStop exit 2 blocks on this version and that the derived transcript path (parent dir / parent stem / subagents / agent-id) resolves. |
| 2026-09-04 | 2.1.260 | Claude writes `~/.claude/settings.json` in auto mode (Bash heredoc, Python `write_text`, Write tool) | BLOCKED by the auto-mode classifier on every route. Scratchpad scripts that assigned `permissions.deny` / `autoMode` keys, or wrote prose about doing so into this file, were blocked as well (content-aware). Writing the project's own `.claude/settings.json` via the Write tool succeeded; the Edit tool on this file succeeded. |
| 2026-09-04 | 2.1.260 | `install.ps1` from a Bash tool call in auto mode (writes `~/.claude/skills`, `~/.claude/agent-library`) | OK - ran unprompted, installed the three changed files, wrote `.backup.<ts>` copies. |
| 2026-09-04 | 2.1.260 | `no-ask` blocks `AskUserQuestion` in a session that ran `/lost-mary` | **VERIFIED LIVE 2026-09-16 by `friction.py --since 2026-09-04`: 6 menus blocked by `no-ask` across three projects (`c--repo-powercountant` 3, `c--repo-lost-mary` 2, the home project 1); the 9 menus still asked were in sessions that had not run `/lost-mary`, which is the designed scope.** The note below is what the suite alone proved, kept as written: `tests/test-hooks.py` covers the script; the docs do not say whether `PreToolUse` fires for this tool. To verify: fresh session, `/lost-mary <anything>`, then ask the lead to present an option menu - expect the block message in its reply, not a menu. Record the result here. |
| 2026-09-04 | 2.1.260 | `no-punt` bounces a lead message containing "I'll leave that for you" | **VERIFIED LIVE 2026-09-06 15:41Z: the transcript of session `355c56ad` holds a `user` record beginning `Stop hook feedback:` whose bracketed command is `no-punt.py` and whose text is `Nothing is left on the table (no-punt). Your final message hands work back to the user: "I'll leave the merge of PR #4 for you"` - found 2026-09-16 while discovering that `friction.py` had never counted it (a Stop block is not an attachment; see the 2026-09-16 row on that shape).** The note below is what the suite alone proved, kept as written: `tests/test-hooks.py` covers the script (hand-backs blocked - pronoun, noun phrase or backticked object; closing, quoted and negated messages allowed; one-strike; streamed-message fallback). To verify: ask the lead to end a turn with that sentence; expect the bounce and a re-emitted message. |
| 2026-09-06 | 2.1.260 | `no-punt` on a real `Stop`, wired live | OK - a turn deliberately ended with "I'll leave the merge of PR #4 for you" was blocked once; the model received the bounce (header, quoted phrase, the three ways to close, the re-emit rule) and the re-emitted stop went through. The first attempt the day before was NOT blocked: the object pattern capped at four words and excluded '#', so 'the merge of PR #4' slipped past - widened the same day (`d4a48aa`), the sentence is a test. Also confirmed: the `Stop` hook runs in the lead session and its stderr reaches the model. |
| 2026-09-05 | 2.1.260 | `ledger record` on a real subagent transcript from this machine (review agent, 23 tool uses) with a synthetic payload | OK - entry carried role, description and branch from `.meta.json` / records, the prompt head, and the correct empty edited/ran sets for a read-only role. Independent review the same day found the file path fields unsanitised (a crafted `file_path` could forge an entry) and the in-repo location unsafe; both fixed before any live wiring. Live firing on SubagentStop and the SessionStart injection are NOT yet verified: wire both, spawn an `implement`, start a new session, expect the "Ledger for" block. |
| 2026-09-05/06 | 2.1.260 | `ledger record` on `Stop` (the lead's own turn), wired live at 16:20 on 09-05 | OK - the wiring turn itself produced `20260905T142204Z-lead-c98fd40.md` under `~/.claude/agent-library/ledger/c--repo-lost-mary/` with the right prompt, branch, edited files and runs; by 09-06 08:36 two other projects held 13 lead entries written by unrelated sessions (night-slot and heartbeat runs). The first real entries exposed two defects, fixed the same day: a chained shell call shares one result, so an early failure masked a later pass (latest outcome now wins), and scratchpad/home paths printed in full (now `scratchpad/<name>`, `~/...`). |
| 2026-09-06 | 2.1.260 | `ledger record` on `SubagentStop`, wired live | OK - a one-minute read-only `explore` spawn (sonnet, its default at the time) produced `...-ac2aaf7500a4.md` under `~/.claude/agent-library/ledger/c--repo-lost-mary/` within seconds of stopping: role and description from `.meta.json`, the prompt head, empty edited/ran for a read-only role, `claimed` = its two-line answer. |
| 2026-09-06 | 2.1.260 | `no-ask` in a real `/lost-mary` session | First attempt NOT blocked: a skill invocation is recorded as `<command-message>` before `<command-name>` and the detector accepted only a leading `<command-name>`. Fixed (tags anywhere in the leading tag block), reinstalled, second `AskUserQuestion` call BLOCKED with the hook's message. `PreToolUse` fires for `AskUserQuestion` - confirmed. |
| 2026-09-06 | 2.1.260 | `check-spawn` on a real `Agent` call | OK - an `implement` spawn with a one-line brief was blocked before starting; the model received `MISSING: OWNED, OFF-LIMITS, DONE MEANS, VALIDATION` and the pointer to the spawn block. |
| 2026-09-06 | 2.1.260 | `ledger attach` on a real `Agent` return | First attempt: entry written, nothing attached - `tool_response` is a structured object and attach read only strings. Fixed (`1ff73a8`); second attempt: the entry appeared in the lead's context as `PostToolUse:Agent hook additional context`. |
| 2026-09-07 | 2.1.261 | Can `permit` be reached without Manual mode? | NO. `permit` allows the exact command offline. A headless `claude -p --permission-mode default --model opus` session in this repo was told to run `git push -u origin permit-probe`: the call was denied ("This command requires approval"), the session transcript recorded only `SessionStart` hook firings, and an invocation log added to the installed `permit.py` stayed empty. The documented "runs these hooks when it cannot prompt" path does not cover `-p` sessions on this version. Incidental finding: the first attempt on the session's default model returned "You've hit your monthly spend limit. Switch to another model to continue." |
| 2026-09-15 | 2.1.272 | Same question, re-asked after 2.1.268 | **YES - the 2026-09-07 NO is retracted.** 2.1.268 fixed "PermissionRequest hooks not firing in `--print` mode", so the earlier measurement tested a bug, not the design. Probe: a sandbox settings file wiring a logger on `PermissionRequest` (delegating to `permit.py`) plus, as a control, the same script on `PostToolUse`. The control fired, proving the wiring, while four allowed commands raised no event; adding `permissions.ask` for `Bash(ruff *)` produced `{"event": "PermissionRequest", "mode": "auto", "cmd": "ruff check good.py", "permit_stdout": "{...\"behavior\": \"allow\"}"}` and the command ran. Lesson recorded twice now: a silent hook is not evidence of an unreachable hook - wire a control on an event you know fires before concluding anything. |
| 2026-09-07 | 2.1.260 | `check-evidence --lead` on a real `Stop`, wired live | OK - the first live block: a turn that edited `~/.claude/settings.json` and `model-policy.md` and ran its suites through a `for` loop was bounced with "You changed 2 file(s) this turn but ran no validation". Correct by its own rules (the loop hid the test paths from `classify()`), one strike, and the re-emission after running the suites explicitly went through. The lead is now checked like every subagent. |
| 2026-09-06 | 2.1.260 | `ledger brief` on `SubagentStart`, wired live | OK - a read-only `explore` spawn reported that its starting context began with "Recent ledger for this project" and quoted the newest entry's header. SubagentStart `additionalContext` reaches the subagent, as documented. |
| 2026-09-15 | 2.1.272 | What a `PostToolUse` payload actually carries, ahead of building `witness.py` (probe hooks on both events, payload dumped) | Three facts, all now relied on. (1) `PostToolUse` **fires inside subagents**: the payload carries `agent_id` (stable across every call of one subagent, e.g. `a2930f3e80ad5d652`) and `agent_type` (e.g. `general-purpose`), plus `cwd`, `duration_ms`, `effort`, `permission_mode`, `prompt_id`, `scratchpad_dir`, `session_id`, `tool_input`, `tool_name`, `tool_response`, `tool_use_id`, `transcript_path`. On the LEAD's own calls there is no `agent_id` and no `agent_type` - only `session_id` and `prompt_id`. (2) A Bash `tool_response` on success is `{stdout, stderr, interrupted, isImage, noOutputExpected}` and has **no `exit_code`**, although the docs claim one. (3) A non-zero exit does not arrive as `PostToolUse` at all: it arrives as **`PostToolUseFailure`**, with no `tool_response` and an `error` string whose first line is `Exit code N` and whose remaining lines are the output (e.g. `Exit code 1\ncollected 3 items\n1 failed, 2 passed`), plus `is_interrupt`. A compound command that swallows its own exit code (`cmd; echo EXIT=$?`) exits 0 and arrives as `PostToolUse` - the same blind spot the transcript path has. The `Stop` payload's `prompt_id` is the one on that turn's `PostToolUse` payloads, which is how a consumer slices a lead turn. |
| 2026-09-15 | 2.1.272 | `check-evidence` reads the witness file instead of the transcript | **VERIFIED LIVE 2026-09-15 evening, twice: the next row observes the id link, and the `Stop` hook then bounced the lead's own closing report because `witness-lead.jsonl`, created after the validation run, held no test command - the consumer was reading the witness file, not the transcript, and the re-run cleared it.** Written before that, kept as the record of what the suite alone proved: What the suite pins, each case failing against the previous version: a transcript holding the edit but not yet the run (the lag this removes) is overruled by a witness file holding both; the witness file wins each disagreement in both directions (transcript failed / witness passed, and the reverse); a `PostToolUseFailure` line blocks a "tests pass" claim; an interrupted call is not a pass; a success with a null `exit_code` is one; a torn line between two valid ones is skipped; a `PostToolUse` edit line is a confirmed edit and a `CHANGED` claim naming it is accepted; the lead's turn is sliced by `prompt_id`, and is EMPTY when the payload carries none (the last turn in the file is the previous one, already judged); a file that exists but holds no parseable line is broken, not evidence, and the transcript decides; an unreadable one says so once on stderr while an absent one falls back in silence; a command is recorded unbounded, so a `&& pytest -q` in the middle of a 2500-char compound is still classified as a run; `prune` removes only directories that are provably witness output, aged by their newest file. One case is end to end: the real `witness.py` writes an `Edit`, a passing `pytest -q` and then a `PostToolUseFailure` for the same command, and the real `check-evidence.py` allows the honest report, blocks the same claim once the failure lands, and allows the report of that failure. To verify live: wire both hooks, have an `implement` edit a file and run a check that fails, and expect the bounce while `~/.claude/agent-library/evidence/<session>/witness-<id>.jsonl` holds the lines it quoted. **The link no test reaches** is that the `SubagentStop` payload's `agent_id` / `session_id` are byte-identical to the `PostToolUse` ones: if they are not, `witness_path()` points at nothing, the transcript is read instead, and the feature is inert behind a fallback that looks exactly like "not wired". Compare the id in the witness file's name with the agent id the ledger wrote from the same stop - that is the whole check. |
| 2026-09-15 | 2.1.272 | Is the `agent_id` on `SubagentStop` the same one `PostToolUse` put in the witness file name? (the gap in the row above) | **YES - observed live, 20:26.** `witness.py` was wired into the live `~/.claude/settings.json` (`install.ps1 -Verify`: 15 hooks, 0 DRIFT; `friction.py --health`: both events "ok - exists, parses"). A spawned `general-purpose` subagent ran one Bash call; its evidence landed in `witness-a19d02625236467bd.jsonl`, and the ledger's `SubagentStop` entry for the same spawn recorded `a19d02625236467bd` - byte-identical, so `witness_path()` resolves and the consumer runs on hook evidence in live sessions, not only in the suite. The lead's own calls landed in `witness-lead.jsonl` under this turn's `prompt_id`; the settings reload took effect during the very call that applied the file. |
| 2026-09-16 | 2.1.273 | Where the Fable allowance went: 48% consumed in a day and a half with the lead on fable | Every `assistant` transcript record carries `message.model` and `message.usage`; one API response is written as several records sharing one `message.id`, so dedupe on it. Summed since 2026-09-15 over 530 files and 2,174 messages: lead on fable 302 msgs, 457K output tokens, 102M cache-read tokens (337K of context per message); explore/review spawns on fable 112 msgs, 13K output, 11M cache-read (98K per message). On any weighting of output against context the lead was 90-97% of fable tokens and the spawns 3-10%. Consequence: `model-policy` puts the lead on opus and leaves `explore`/`review` on fable, which keeps the judgment where it was and removes about nine tenths of the burn; with the lead on opus the Fable cap can only end a spawn (`Agent terminated early due to an API error` - respawn with `model: opus`) instead of silencing the session behind the consent prompt. `scripts/usage.py --since <date>` is the measurement; re-run it before the next policy change. |
| 2026-09-16 | 2.1.273 | How a `Stop` hook's block is recorded in the transcript (asked because `friction.py` dated `check-evidence.py --lead` last at 2026-09-06 with 0 blocks, while this session had been bounced by it twice on 2026-09-15) | **Not as an attachment.** A blocked `Stop` is a `user` record - `type: user`, string `message.content` - that begins `Stop hook feedback:\n[<the hook command exactly as written in settings>]: <the hook's stderr text>`. `hook_blocking_error` attachments exist for tool-event hooks only (this session: 60 `PostToolUse`, 1 `PostToolUseFailure`); its four `hookEvent: Stop` attachments are `hook_success` notices from `ledger.py record`. Across every transcript on disk: 60 such records, all string-form, all `Stop` - none for `SubagentStop` yet, so that shape is unmeasured. A transcript can hold the same record twice with the same `uuid` after a resume, so a consumer dedupes on `uuid`; and a `tool_result` that merely quotes the phrase (list-form content) is not one. Consequence: `friction.py` had never counted a Stop block - in this session's transcript 3 `check-evidence --lead` blocks and the one live `no-punt` block, machine-wide 52 and 3 out of 66 recorded Stop firings, were invisible (the attachment path had counted 0 Stop blocks, so none exists in that shape) - fixed 2026-09-16, the record shape pinned by six cases in `tests/test-hooks.py` that failed against the previous version. |
| 2026-09-17 | 2.1.273 | How often does the lead end a turn by asking for a go-ahead, and does `no-punt` see it? (asked because the user reported "it solves one item, then waits for my confirmation") | Surveyed every lead transcript on this machine since 2026-09-01: 1,646 turn-ending messages (a text-only message followed by a real user record, or the end of the file). 279 ended on a go-ahead request by a narrow regex survey; the new detector, run over the full messages, finds 474. `no-punt` had bounced 5 of them - its patterns covered "I'll leave that for you" shapes only. The phrasing actually used: "Say the word and I'll...", "Want me to...?", "If you want, I'll...", "Standing by", "your call", "Which first?", "Remaining: ...". 160 were answered with a prompt, 69 of those under 40 characters: "go", "yes please", "then do it?", "why do you ask for my permission?", "don't ask me again just work". TodoWrite appears in none of the 116 transcripts (off by default since 2.1.233 for these models), so a todo-based gate has nothing to read. Consequence: `no-punt` judges the closing paragraphs for go-ahead requests, leftover lists and closing questions, requires `DONE:` / `IN FLIGHT:` / `BLOCKED:` under `/lost-mary` on a turn that called tools, bounces up to two idle / six per turn instead of once, and `friction.py` counts turn-ending hand-backs (474 since 09-01, 5 bounced, 0 `BLOCKED:`) as the metric to watch. |
| 2026-09-17 | 2.1.273 | What the working keep-going designs share (research; sources in `AGENTS.md`) | A completion token that the Stop hook demands, not phrase matching: the `ralph-wiggum` plugin blocks until `<promise>DONE</promise>`, `taskmaster` until `TASKMASTER_DONE::<session>`, `/goal <condition>` until a fresh evaluator says met or impossible; all bound the loop (`--max-iterations`; Claude Code's own cap of eight consecutive blocks without progress, raised with `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`). Documented pitfalls: the model rephrases around phrase lists and drifts to minimal "holding" replies under repeated blocks; a guard keyed on `stop_hook_active` alone once let a hook loop for 50 minutes (the flag arrived false with system reminders interleaved, issue 54360); every block re-bills the whole context (one report: 4 blocks, $3.25). Anthropic's own wording for agentic persistence: "complete tasks fully ... Never artificially stop any task early", "Make routine judgment calls yourself, and check in only when different readings of the request would lead to materially different work", the one legitimate stop being an action that is hard to reverse or affects shared systems. Adopted: the three closing lines as the token, `BLOCKED:` as the escape hatch, the guard counted from the transcript, the bounce restating the task and the `Done means` line (what `/goal` does with its condition). Not adopted: `/goal` itself (first-party, untried here - a complement for long runs), a state file, TodoWrite. |
| 2026-09-17 | 2.1.273 | `no-punt` on a real `Stop`, the go-ahead class, wired live | OK - a turn deliberately ended with "Want me to continue with their findings when they land?" (session `355c56ad`, 08:27Z) was blocked. The model received the header `The turn is not over (no-punt).`, the quoted phrase `'Want me to'`, the rule, the `Close the turn with DONE: / IN FLIGHT: / BLOCKED:` line (the session had run `/lost-mary`), its own `Done means` framing from the start of the turn (cut at 200 characters) and the task text taken from the `<command-args>` of the `/lost-mary` prompt. The feedback record carries the prompt's own `promptId` and `isMeta: true` (8 of 8 sampled across four sessions) - what the loop guard counts on. `friction.py --since 2026-09-17` then showed the bounce (`no-punt.py 2 firing(s), 2 block(s)`) and listed the message under hand-back examples as `go-ahead 'Want me to'`. `check-evidence --lead` blocked the same stop for an unrelated reason (a chained shell call had died on a script assertion and taken a `ruff check` down with it): two Stop hooks blocking one stop both reach the model. |
| 2026-09-17 | 2.1.274 (the eval child) | Do the plugin's hooks run inside `claude plugin eval` on Windows? (asked because the `keep-going` with-arm scored higher without a single bounce in its transcripts) | **NO - every plugin hook had failed open in every reading since 09-15.** The with-arm transcripts hold `hook_non_blocking_error` attachments for all three Stop hooks: `exitCode 1`, stderr `NoInstallsError: No runtimes are installed. Try running "py install default" first` from the Python Install Manager. The sandbox redirects HOME and AppData, and `python3` on this machine's PATH is that manager's shim under `WindowsApps`, whose runtime registry lives under the real `AppData\Local\Python` - inside the sandbox it finds nothing. Also measured: a bare `bash` resolves through the Windows PATH to the WSL stub in `System32` ("execvpe(/bin/bash) failed") even from inside Git's own bash started non-login, while `/bin/bash` there is Git's bash. Fix: `hooks/run.sh`, invoked as `/bin/bash "${CLAUDE_PLUGIN_ROOT}/hooks/run.sh" <script>` in `hooks/hooks.json` (string form, run in Git Bash like every other hook command string), tries `$LOST_MARY_PYTHON`, `python3`, `python`, then the user's `pythoncore-3*` runtime directories, and fails open with a notice; `tests/test-hooks.py` runs a payload through it. Verified by emulation: the command through Git's `bash.exe -c` bounced a hand-back payload (exit 2) with a backslash root, a forward-slash root, and with HOME and LOCALAPPDATA pointed at a directory that does not exist. Consequence: the three `spawn-contract` readings and the first `keep-going` reading measured the agents alone; whatever delta they showed is not the hooks' doing. |
| 2026-09-17 | 2.1.273 | The installed hook in the first hour, across the other live sessions on this machine (not the session that built it) | Five bounces in three projects between 08:27Z and 08:42Z, none a false positive. `c--repo-EU`: "Want me to run the health check again, or check whether a reboot happened overnight?" was bounced and the next message began `Assumption: both checks are read-only and cheap, so I'll just run them rather than ask.`; "say the word" (a rebuild waiting for approval) was bounced and the next message began `The hook is right - you told me to take care of it.` and went on to try the write another way; two `/lost-mary` turns that had edited and run things ended without a closing line and were bounced as `unclosed` - one had ended on a bare backticked `DONE:` with nothing after it - and both continued with tool calls. `c--repo-powercountant`: a turn that ended mid-work ("The audit suite is running; one more round, then I commit") was bounced as `unclosed` and continued with a tool call. Change made from this evidence: the closing-line regex accepts backticks around the token but still requires content after the colon. `friction.py --since 2026-09-17`: `no-punt.py 10 firing(s), 10 block(s)` machine-wide by 08:43Z. |
| 2026-09-17 | 2.1.274 (the eval child) | `no-punt` inside `claude plugin eval`, through `hooks/run.sh` | OK - the `keep-going` case: `with 1.00  without 0.44  delta +0.56`, $0.59. One with-arm run needed two turns and its sandbox transcript holds the bounce as a `Stop hook feedback:` user record whose bracketed command is the `/bin/bash ".../hooks/run.sh" ".../scripts/no-punt.py"` string from `hooks.json` - so the plugin form records a bounce exactly like the installed form, with the shim's command in the brackets, which is why own-bounce recognition also accepts the hook's header. A hook that succeeds quietly still leaves no attachment: a with-arm sandbox with no bounce is indistinguishable from a without-arm one by attachments alone. |
| 2026-09-06 | 2.1.260 | `ledger recall` on `SessionStart` | OBSERVED - `friction.py --since 2026-09-04` (run 2026-09-16) counts 48 `ledger.py recall` firings on `SessionStart`, last 2026-09-16, and this session's compaction injected the "Ledger for c--repo-lost-mary" block (last 2 of 38 entries) as context. |
| 2026-09-24 | 2.1.281 | `gh pr merge` from a lead session in auto mode, before a merge rule existed in `autoMode.allow` | DENIED `[Merge Without Review]` - four times in one `c--repo-EU` session on 2026-09-24, twice in `c--repo-powercountant` on 2026-09-23 after six `review` spawns; a chained `push && pr create && pr merge` on 2026-09-23 was denied whole. One merge passed on 2026-09-23 after the user named it - consistent with `soft_deny` (the inference in the auto-mode note above). |
| 2026-09-24 | 2.1.281 | `gh pr merge` under the template's third `autoMode.allow` rule (the user pasted it into `~/.claude/settings.json`; a trailing comma broke the file once - "Expected object, but received undefined") | VERIFIED LIVE 11:42Z: `gh pr merge 18 --repo frederikgnie/lost-mary --merge --delete-branch`, a lone Bash call after a `review` spawn and the three test suites, went through with no prompt. Whether the classifier checks the review-spawn precondition or treats the rule as a blanket merge exception is not yet separated: no merge has been tried under the rule without one. |
